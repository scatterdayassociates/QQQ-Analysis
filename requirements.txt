# Smart Money Pipeline - Cloud Functions compatible
# Removed: fastapi, uvicorn, gunicorn (Cloud Functions provides HTTP server)
# Kept: apscheduler (PipelineOrchestrator needs it for initialization)

psycopg2-binary==2.9.9
python-dotenv==1.0.0
requests==2.31.0
pydantic==2.5.0
pydantic-settings==2.1.0
numpy==1.24.3
pandas==2.0.3
google-cloud-logging==3.5.0

# Scheduling (needed by PipelineOrchestrator for initialization)
apscheduler==3.10.4

# Cloud SQL connectivity (handles auth and connection pooling for Cloud Functions)
cloud-sql-python-connector==1.4.3
